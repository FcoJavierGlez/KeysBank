{
    const init = () => {
        const LOGIN_SCREEN = [...document.getElementsByClassName("login_screen")];
        console.log(LOGIN_SCREEN);
    }
    document.addEventListener("DOMContentLoaded", init);
}